{
  "name": "puppet-string",
  "version": "0.3.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "puppet-string": "./bin/puppet-string.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.3.2",
    "cross-spawn": "^6.0.5",
    "jest": "^23.6.0",
    "puppeteer": "^1.2.0",
    "serve": "^6.5.3"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "<rootDir>/scripts/",
      "<rootDir>/node_modules/"
    ]
  }
}
